server:
  port: 8081

spring:
  datasource:
    name: shopping
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1/shopping?useUnicode=true&characterEncoding=utf8&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:interface/*.xml  #mapper文件
  type-aliases-package: com.zhongwen.shopping.bean
